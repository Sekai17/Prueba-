// Bug Report Auto-Generator — Sekai Edition
// Purpose: Quickly create consistent, high-quality bug reports with minimal manual effort.
// Language: JavaScript (Node.js or Browser)

function generateBugReport({
  title,
  description,
  steps,
  expected,
  actual,
  impact,
  device,
  os,
  appVersion,
  model,
  attachments = [],
  extraInfo = ""
}) {
  return `
[BUG REPORT]

**Title:** ${title}

**Description:**
${description}

**Steps to Reproduce:**
${steps.map((s, i) => `${i + 1}. ${s}`).join("\n")}

**Expected Result:**
${expected}

**Actual Result:**
${actual}

**Impact:**
${impact}

**Environment:**
- Device: ${device}
- OS: ${os}
- App Version: ${appVersion}
- Model: ${model}

**Attachments:**
${attachments.length ? attachments.join("\n") : "(none)"}

**Extra Info:**
${extraInfo || "(none)"}

---
Generated by Sekai's Auto Bug Reporter
`;
}

// Example usage:
const report = generateBugReport({
  title: "GPT-5 Thinking stops when app in background",
  description: "When using GPT-5 Thinking, moving the app to background interrupts the reasoning and shows an offline error despite stable internet.",
  steps: [
    "Open ChatGPT app on Android",
    "Select GPT-5 Thinking",
    "Send a long prompt",
    "Switch to another app for 20 seconds",
    "Return to ChatGPT"
  ],
  expected: "Reasoning continues in background and result is displayed when returning.",
  actual: "Shows 'Looks like you're offline' and response is lost.",
  impact: "Blocks multitasking and affects perception of speed in Thinking mode.",
  device: "Motorola Moto G",
  os: "Android 10 (build K)",
  appVersion: "1.2025.207",
  model: "GPT-5 Thinking",
  attachments: ["screenshot.png"],
  extraInfo: "Also occurs on mobile browser (Brave, Android 10)."
});

console.log(report);
